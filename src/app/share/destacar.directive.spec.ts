import { DestacarDirective } from './destacar.directive';

describe('DestacarDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarDirective();
    expect(directive).toBeTruthy();
  });
});
